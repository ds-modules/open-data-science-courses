# open-data-science-courses
A centralized repository of adoptable data science courses, including Canvas shells, autograders, and links to our edX offerings.

## Local preview

You must run these commands **from the repo root** (the folder that contains `myst.yml`). If you see **"No site configuration found"**, the build is running in the wrong directory or `myst.yml` is missing.